@model List<Skill>

<div class="col-six tab-full right">
	<h3>Yeteneklerim</h3>

	<ul class="skill-bars">

		@foreach (var item in Model)
		{
			<li>
				<strong>@item.Title</strong>
			</li>
		}
	</ul>
</div>